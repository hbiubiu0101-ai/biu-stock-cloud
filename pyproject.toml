[project]
name = "biu-stock-cloud"
version = "0.1.0"
description = "Biu stock workspace"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "streamlit>=1.61.1",
    "pandas",
    "numpy",
    "requests",
    "plotly",
    "matplotlib",
    "akshare",
    "baostock",
    "tushare",
    "supabase",
]

[tool.uv]
package = false
